# Build products. The mex binaries are checked in deliberately so the package
# is usable without a compiler; the intermediates are not.
PsychMetalCore.octave.o
PsychMetalCore.matlab.o
dist/

# macOS
.DS_Store

# Measurement output, regenerated by the tests
*.csv
*.mat
